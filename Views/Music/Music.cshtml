@{
    ViewData["Title"] = "Music";
}

<head>
    <meta charset="utf-8" />
    <script
        src="https://code.jquery.com/jquery-3.5.1.min.js"
        integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
        crossorigin="anonymous"></script>
    <script>
        $(function () {
            $('#btn-one').on('click', function () {
                console.log("getting..");
                $.get(
                    'https://localhost:5001/Music/',
                    function (response) {
                        console.log("> ", response);
                        $("#result").html(response);
                });
              
            });
        });
          $(function () {
            $('#btn-two').on('click', function () {
                console.log("getting..");
                $.get(
                    'https://localhost:5001/Music/NotAfraid/',
                    function (response) {
                        console.log("> ", response);
                        $("#result").html(response);
                });
              
            });
        });
          $(function () {
            $('#btn-three').on('click', function () {
                console.log("getting..");
                $.get(
                    'https://localhost:5001/Music/RiseUp/',
                    function (response) {
                        console.log("> ", response);
                        $("#result").html(response);
                });
              
            });
        });
          $(function () {
            $('#btn-four').on('click', function () {
                console.log("getting..");
                $.get(
                    'https://localhost:5001/Music/AllTheStars/',
                    function (response) {
                        console.log("> ", response);
                        $("#result").html(response);
                });
              
            });
        });
    </script>
</head>

<body>
    <br />
    <button id="btn-one">Music Lyrics</button>
    <br />
    <button id="btn-two">Not Afraid</button>
    <br />
    <button id="btn-three">Rise Up</button>
    <br />
    <button id="btn-four">All The Stars</button>
    <br />
    <div id="result"></div>
</body>
